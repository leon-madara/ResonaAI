"""ResonaAI shared Python package.

Purpose:
- Provide a stable import boundary for shared modules used across services.

Notes:
- Shared code under `src/` is intended to be imported as `src.<module>`.
"""
